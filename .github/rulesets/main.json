{
  "name": "Repository checks for main",
  "target": "branch",
  "enforcement": "active",
  "bypass_actors": [],
  "conditions": {
    "ref_name": {
      "exclude": [],
      "include": [
        "~DEFAULT_BRANCH"
      ]
    }
  },
  "rules": [
    {
      "type": "required_status_checks",
      "parameters": {
        "required_status_checks": [
          {
            "context": "Analyze (python)"
          },
          {
            "context": "Analyze (actions)"
          }
        ],
        "strict_required_status_checks_policy": true,
        "do_not_enforce_on_create": true
      }
    }
  ]
}
